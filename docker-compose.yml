services:
  nerugaysyabot:
    build: .
    image: sakameow/nerugaysyabot:latest
    container_name: nerugaysyabot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/data
