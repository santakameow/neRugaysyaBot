# ne Rugaysya Bot

a simple telegram bot that detects bad words and warns.

## docker

build

```sh
docker build -t nerugaysyabot .
```

run

```sh
# create a env before btw
docker run --name nerugaysyabot -d nerugaysyabot
```
